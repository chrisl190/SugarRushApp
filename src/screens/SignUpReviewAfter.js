import React from 'react';
import {View, ScrollView} from 'react-native';

const SignUpReviewAfter = ({navigation}) => {
  return (
    <ScrollView showsVerticalScrollIndicator={false}>
      <View></View>
    </ScrollView>
  );
};

export default SignUpReviewAfter;
